/**
 * Modelo de Token de Recuperación
 * Maneja tokens para recuperación de contraseñas
 * Responsable: David
 */

// TODO: Definir estructura de token
// - Campos: token, userId, expiresAt, isUsed, createdAt
// - Métodos: create, findByToken, markAsUsed, deleteExpired

module.exports = {
    // Operaciones de tokens de recuperación
};
